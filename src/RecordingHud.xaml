<Window x:Class="WinSnipper.RecordingHud"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        Left="-10000" Top="-10000">
    <Window.Resources>
        <Style x:Key="HudBtn" TargetType="Button">
            <Setter Property="Foreground" Value="#FFE8EAEE"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Width" Value="30"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="B" Background="Transparent" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="B" Property="Background" Value="#FF3C3E48"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border Background="#F21C1D22" CornerRadius="14" Padding="12,5"
            BorderBrush="#4DFFFFFF" BorderThickness="1"
            MouseLeftButtonDown="Bar_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="1" Opacity="0.45" Color="#FF000000"/>
        </Border.Effect>
        <StackPanel Orientation="Horizontal">
            <Ellipse x:Name="RecDot" Width="9" Height="9" Fill="#FFE5484D" VerticalAlignment="Center"/>
            <TextBlock x:Name="TimeText" Text="0:00" Foreground="#FFE8EAEE" FontSize="13"
                       FontFamily="Consolas" VerticalAlignment="Center" Margin="9,0,10,0" MinWidth="38"/>
            <Button x:Name="PauseBtn" Style="{StaticResource HudBtn}" Content="⏸" ToolTip="Pause"
                    Click="Pause_Click"/>
            <Button Style="{StaticResource HudBtn}" Content="⏹" ToolTip="Stop and save"
                    Foreground="#FFE5484D" FontSize="14" Click="Stop_Click" Margin="2,0,0,0"/>
        </StackPanel>
    </Border>
</Window>
